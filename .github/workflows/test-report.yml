name: 'Test Report'

on:
  workflow_call:

jobs:
  report:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      actions: read
      checks: write
    steps:
      - uses: dorny/test-reporter@v2
        with:
          artifact: test-results
          name: Kotest Tests
          path: "**/*.xml"
          reporter: java-junit
